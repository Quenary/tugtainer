import { WithHostsListDirective } from './with-hosts-list.directive';

describe('WithHostsListDirective', () => {
  it('should create an instance', () => {
    const directive = new WithHostsListDirective();
    expect(directive).toBeTruthy();
  });
});
